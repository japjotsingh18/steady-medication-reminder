# Copy this file to .env.local for local-only values.
# Steady currently needs no API keys or application secrets.
#
# Cloudflare D1 is connected through the DB binding in wrangler.jsonc.
# Real credentials must be added with `wrangler secret put NAME` and must
# never be committed to this repository.
